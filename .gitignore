# ---- Simulation outputs (reproducible, large — do NOT commit) ----
out_*/
*_out/
runs/
poc_radiograph_out/
poc_cone_film_out/
**/object/
**/flat/
config.json

# ---- Heavy data products ----
*.root          # phase-space (uproot) — tens of MB each
*.raw
*.zraw
*.mhd
*.zip

# ---- Keep PNG previews out by default (uncomment to allow) ----
# *.png

# ---- Python ----
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
.ipynb_checkpoints/

# ---- Remote worker/server (คีย์ลับอยู่ใน config.json ซึ่ง ignore ไว้ด้านบนแล้ว) ----
remote/worker/worker.log
remote/worker/worker.heartbeat
*.log
remote/server/data*/
remote/server/.env

# ---- macOS ----
.DS_Store

# ---- โฟลเดอร์สำรองโมเดล (local เท่านั้น ไม่ commit — ใหญ่และซ้ำ) ----
backup_model/
models_replaced_backup/

# NOTE: models/*.stl ARE tracked on purpose (original 3D inputs).

# report build artifacts
report/pylibs/
report/_qa_*.png
